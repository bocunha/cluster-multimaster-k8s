- hosts:
  - ec2-k8s-m2
  - ec2-k8s-m3
  become: yes
  tasks:
    - name: "Reset cluster"
      shell: "kubeadm reset -f"

    - name: "Fazendo join kubernetes master"
      shell: 'kubeadm join k8s-haproxy:6443 --token a0l6wt.x97oui7bnuy10tok --discovery-token-ca-cert-hash sha256:0783423964bba0743a2bcf5d64099603e8439d972a65d6c5712dd1fd6958a4e0 --control-plane --certificate-key dcc42e2662b7efd415b306293db10f59fd41899070e18e4632c03c04d4876fb3'

    - name: "Colocando no path da maquina o conf do kubernetes"
      shell: mkdir -p /home/ec2-user/.kube && sudo cp -f /etc/kubernetes/admin.conf /home/ec2-user/.kube/config && sudo chown 1000:1000 /home/ec2-user/.kube/config && export KUBECONFIG=/etc/kubernetes/admin.conf
#---
- hosts:
  - ec2-k8s-w1
  - ec2-k8s-w2
  - ec2-k8s-w3
  become: yes
  tasks:
    - name: "Reset cluster"
      shell: "kubeadm reset -f"

    - name: "Fazendo join kubernetes worker"
      shell: kubeadm join k8s-haproxy:6443 --token a0l6wt.x97oui7bnuy10tok --discovery-token-ca-cert-hash sha256:0783423964bba0743a2bcf5d64099603e8439d972a65d6c5712dd1fd6958a4e0

#---
- hosts:
  - ec2-k8s-m1
  become: yes
  tasks:
    - name: "Configura weavenet para reconhecer os nós master e workers"
      shell: kubectl apply -f "https://cloud.weave.works/k8s/net?k8s-version=$(kubectl version | base64 | tr -d '\n')"

    - name: Espera 30 segundos
      wait_for: timeout=30

    - shell: kubectl get nodes -o wide
      register: ps
    - debug:
        msg: " '{{ ps.stdout_lines }}' "
